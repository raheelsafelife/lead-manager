# Lead Manager Environment Variables

# Email Configuration (SMTP)
# For Gmail:
# 1. SENDER_EMAIL: Your Gmail address (e.g., yourname@gmail.com)
# 2. SENDER_PASSWORD: Your Gmail App Password (NOT your regular password)
#    - If you have 2-Factor Authentication enabled (recommended), you MUST use an App Password
#    - Generate one at: https://myaccount.google.com/apppasswords
# 3. SMTP_SERVER: Use smtp.gmail.com for Gmail
# 4. SMTP_PORT: Use 587 for TLS (recommended) or 465 for SSL
SENDER_EMAIL=<your-email@gmail.com>
SENDER_PASSWORD=<your-gmail-app-password>
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# FastAPI Service Configuration (for SafeLife CCP Form Integration)
LEAD_MANAGER_API_KEY=safelife-ccp-2024-secret-key
LEAD_MANAGER_API_URL=

